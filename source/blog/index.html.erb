---
layout: blog
section: blog
title: Blog
---

<% page_articles.each_with_index do |article, i| %>
    <div>
        <h2>
            <%= link_to article.title, article %>
            <small>&mdash; <%= article.date.strftime('%B %Y') %></small>
        </h2>
        <%= article.summary(250) %>
    </div>
    <!---->
<% end %>
