---
layout: blog
title: Blog
---

<% page_articles.each_with_index do |article, i| %>
    <p>
        <h2 class="header-pair"><%= link_to article.title, article %></h2>
        <h3 class="header-pair"><%= article.date.strftime('%B %Y') %></h3>
    </p>
    <!--<%= article.summary(250) %>-->
<% end %>
